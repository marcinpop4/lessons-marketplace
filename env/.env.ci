# Docker-specific environment variables

# Server configuration
PORT=3000

# Database configuration
DB_HOST=database-ci
DB_PORT=5432
DB_NAME=lessons_marketplace
DB_USER=marcin
DB_PASSWORD=postgres
DB_SSL=false
DB_POOL_SIZE=10
DATABASE_URL=postgresql://marcin:postgres@database-ci:5432/lessons_marketplace?schema=public

# API Configuration
VITE_API_BASE_URL=http://server-ci:3000

# Frontend URL - for regular use
FRONTEND_URL=http://localhost:5173

# JWT configuration
JWT_EXPIRES_IN=1h
REFRESH_TOKEN_EXPIRES_IN=7d

# JWT Secret (for development in Docker)
JWT_SECRET=development_jwt_secret_key_change_in_production 