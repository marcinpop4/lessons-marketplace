# Docker-specific environment variables

# Database configuration
DB_HOST=database
DB_PORT=5432
DB_NAME=lessons_marketplace
DB_USER=marcin
DB_PASSWORD=postgres
DB_SSL=false
DB_POOL_SIZE=10
DATABASE_URL=postgresql://marcin:postgres@database:5432/lessons_marketplace?schema=public

# API Configuration
VITE_API_BASE_URL=http://server:3000

# Frontend URL - for regular use
FRONTEND_URL=http://localhost:5173

# Frontend URL specifically for container-to-container communication (used by tests)
DOCKER_FRONTEND_URL=http://frontend:80

# JWT Secret (for development in Docker)
JWT_SECRET=development_jwt_secret_key_change_in_production 